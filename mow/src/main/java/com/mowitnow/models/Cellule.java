package com.mowitnow.models;

/**
 * @author Ashfack
 * 
 *         Cellule de la pelouse
 */
public enum Cellule {
	/**
	 * Etat d'une cellule (contient de l'herbe par défaut), Tondeuse (si occupée par
	 * une tondeuse), tondue (si a été tondue)
	 */
	HERBE, TONDEUSE, TONDUE;
}
